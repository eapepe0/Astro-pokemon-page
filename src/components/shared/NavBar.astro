---
const links = [
    {
        href:"/", text:"Listado"
    },
    {
        href:"/pokemons/1", text:"Paginado"
    },
    {
        href:"/favorites", text:"Favoritos"
    },
]

const currentPath = Astro.url.pathname  // obtenemos el path donde estamos

---

<!-- component -->
<nav class="shadow bg-slate-800">
    <div class="container flex items-center justify-center p-6 mx-auto text-gray-600 capitalize dark:text-gray-300">

        {
            links.map(({href , text})=>(
                <a transition:name="menu-line" {href} class:list={[
                    "text-gray-800 dark:text-gray-200 mx-1.5 sm:mx-6",
                    {
                        "border-b-2 border-blue-500" : currentPath === href
                    }
                ]}>{text}</a>
            ))
        }
       
    </div>
</nav>